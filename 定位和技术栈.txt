





推送成功。验证 raw 直链成活（install.sh + 中文文件名的保活清单——后者 URL 编码最容易出问题）：
% WebFetch https://raw.githubusercontent.com/THATSCODEGUY/PHONE-LOCATION-SCRIPT/main/phone-termux/install.sh
% WebFetch https://raw.githubusercontent.com/THATSCODEGUY/PHONE-LOCATION-SCRIPT/main/phone-termux/HYPEROS-%E4%BF%9D%E6%B4%BB%E6%B8%85%E5%8D%95.md
GitHub 侧全部完成 ✅
检查项	结果
首次推送	main 分支已建立，4 个 commit 全部上库（v1.0.0 → v2.2.1）
raw 直链 · install.sh	✓ 正常服务，真实仓库地址已生效
raw 直链 · 保活清单（中文名+URL编码）	✓ 正常服务
本地工作区	干净——本轮只是推送，无文件改动，无需新 commit
仓库地址：https://github.com/THATSCODEGUY/PHONE-LOCATION-SCRIPT （以后每次改代码，git push 一条命令即同步）
接下来轮到你的三件事
① Supabase（约 5 分钟）
- supabase.com → New project（区域选香港或新加坡，数据库密码设一个记住的）
- SQL Editor → 粘贴 server/supabase/schema.sql 全文 → Run → 绿色 Success
- Settings → API → 抄下两样：Project URL + service_role key（⚠ 是长的那个，不是 anon public）
② 生成两把密钥（1 分钟）
openssl rand -hex 32   # 第1次 → DEVICE_KEY
openssl rand -hex 32   # 第2次 → ACCESS_TOKEN
（Git Bash 可直接跑；PowerShell 用：-join ((1..64) | ForEach-Object { '{0:x}' -f (Get-Random -Max 16) })）
③ Vercel（约 5 分钟）
1. vercel.com → 用 GitHub 登录 → Add New Project → Import PHONE-LOCATION-SCRIPT
2. Root Directory 改成 server（最关键的一步，漏了必失败）
3. Environment Variables 填 4 个：SUPABASE_URL / SUPABASE_SERVICE_KEY / DEVICE_KEY / ACCESS_TOKEN
4. Deploy → 等约 1 分钟 → 拿到 https://xxx.vercel.app
部署完成后把 vercel.app 域名发我（不用发密钥），我帮你做全链路验收；然后手机 Termux 粘一条命令就装好了。


-------------------

你漏了什么（两处）
1. 生成两把自家密钥（Vercel 要填 4 个变量，其中 2 个是你自己造的）：
      openssl rand -hex 32   # 第1次 → DEVICE_KEY（手机用）
   openssl rand -hex 32   # 第2次 → ACCESS_TOKEN（地图网址用）
   2. push 之前要先改 install.sh 的 REPO_BASE → 把仓库地址发我，我一行改掉再 push（否则手机一键安装命令是死链）。
完整顺序（补漏后的终版）
1. GitHub：建空公有仓库 → 地址发我 → 我改 install.sh → 你执行：
      git remote add origin https://github.com/你的用户名/仓库名.git
   git branch -M main
   git push -u origin main
   2. Supabase：建项目（区域选香港/新加坡）→ 跑 schema.sql → 抄下 Project URL + service_role key（⚠ 不是 anon key）
3. 生成两把密钥（上面的 openssl 命令）
4. Vercel：vercel.com 用 GitHub 账号登录 → Add New Project → Import 你的仓库 → Root Directory 改成 server（关键，漏了就部署不出来）→ Environment Variables 填 4 个（SUPABASE_URL / SUPABASE_SERVICE_KEY / DEVICE_KEY / ACCESS_TOKEN）→ Deploy → 拿到 https://xxx.vercel.app
5. 验证：浏览器开 https://xxx.vercel.app/map?token=你的ACCESS_TOKEN，能看到地图就通了（“暂无上报记录”正常，手机还没装）
6. 手机端：F-Droid 装 Termux 三件套 → Termux 粘一条命令（bash -c "$(curl -sSL https://raw.githubusercontent.com/你/仓库/main/phone-termux/install.sh)"）→ 按保活清单逐项设置




整套系统：手机端 Redmi Note 15 Pro 用 Termux 跑 bash 脚本（report.sh 通过 termux-location 取 GPS/网络定位、agent.sh 用 curl 长轮询接命令），按 cron 被动定时+主动按钮双模式，通过 HTTPS 把位置 POST 到 Vercel 上零依赖 Node.js serverless 函数（report/poll/command/locations 四个端点，DEVICE_KEY 与 ACCESS_TOKEN 双密钥时序安全鉴权），数据写入 Supabase Postgres（locations/commands/devices 三表，RLS 零公开策略，service_key 仅存 Vercel 环境变量），浏览器打开 Vercel 托管的 Leaflet+OpenStreetMap 页面 /map?token= 实时查看位置/精度圈/轨迹/心跳在线状态，手机脚本经 GitHub 公有仓库 raw.githubusercontent.com 直链一键安装。


